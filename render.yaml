{
  "$schema": "https://render.com/schema/v2/render-web.yaml",
  "name": "workbuddy-token-stats",
  "runtime": "python313",
  "region": "singapore",
  "startCommand": "python render_server.py",
  "buildCommand": "pip install -r requirements.txt",
  "envVars": []
}
